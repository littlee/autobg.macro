{
  "name": "autobg.macro",
  "version": "1.0.1",
  "description": "a babel macro which can auto-generate background-image, width, height CSS code based on the given image path, usually used with `styled-components`",
  "main": "index.js",
  "keywords": [
    "babel-plugin-macros",
    "styled-components"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-plugin-macros": "^2.8.0",
    "image-size": "^0.9.1"
  }
}
